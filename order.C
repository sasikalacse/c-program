traverse the list for i=0 to n-1 elements
{
check for sign of A[abs(A[i])];
if positive then
make it negative by A[abs(A[i])]=-A[abs(A[i])];
else //i.e..,A[abs(A[i])] is negative
this element (ith element of the list) is repeatation
}
